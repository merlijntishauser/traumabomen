# Traumatrees iOS app

The SwiftUI reflection companion. UI only; all crypto, sync, storage, and the
API client live in the Kotlin Multiplatform core (`../core`), consumed here as
an XCFramework. Design and milestones: `docs/plans/2026-07-05-ios-companion-design.md`.

The Xcode project is generated by [xcodegen](https://github.com/yonaskolb/XcodeGen)
from `project.yml` and is never edited by hand (`Traumatrees.xcodeproj` is
gitignored).

## Run on the simulator

```bash
brew install xcodegen                                   # once
cd mobile/core && gradle assembleTraumabomenCoreDebugXCFramework
cd ../ios && xcodegen generate
open Traumatrees.xcodeproj
```

Pick an iPhone simulator and Run. On the simulator the app talks to the local
dev backend at `http://localhost:8000` (start it with `docker compose up`).

CLI build (no signing needed for the simulator):

```bash
xcodebuild -project Traumatrees.xcodeproj -scheme Traumatrees \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  CODE_SIGNING_ALLOWED=NO build
```

## Run on your iPhone (against production)

Prerequisites: Xcode, an iPhone on iOS 17+, an Apple ID, and an existing
**production account** on traumatrees.org (the app has no sign-up screen;
register on the web first if needed).

1. Build the core and generate the project:
   ```bash
   cd mobile/core && gradle assembleTraumabomenCoreDebugXCFramework
   cd ../ios && xcodegen generate && open Traumatrees.xcodeproj
   ```
2. In Xcode, select the **Traumatrees** target -> **Signing & Capabilities**,
   check **Automatically manage signing**, and choose your **Team** (add your
   Apple ID under Xcode -> Settings -> Accounts if needed). The bundle id
   `org.traumabomen.companion` auto-provisions.
3. Plug in the iPhone (or use wireless debugging), select it as the run
   destination, and Run.
4. First launch only: on the iPhone, **Settings -> General -> VPN & Device
   Management -> trust your developer profile**, then reopen the app.
5. Log in with your production email + password, then your encryption
   passphrase. Face ID works on device (unlike the simulator).

Notes:

- **On a real device the app defaults to `https://www.traumatrees.org/api`;**
  on the simulator it defaults to `http://localhost:8000`. Override either
  with the `-apiBase <url>` launch argument in the scheme (Run -> Arguments).
- A free Apple ID works but expires after 7 days; re-run from Xcode to renew,
  or use a paid developer account.
- To keep your team across `xcodegen generate`, set
  `DEVELOPMENT_TEAM: <10-char Team ID>` in `project.yml` (find it in Xcode ->
  Settings -> Accounts -> Manage Certificates, or the Apple Developer portal).

## Layout

- `Sources/` — SwiftUI views, the `AppModel` state machine, key custody
  (Secure Enclave), the Keychain token store, localization, and the design
  system (`Theme`, `Atmosphere`, `Icons`).
- `Resources/` — bundled fonts, the app icon and launch-screen assets, the
  Lucide icon PDFs, and the hero photography (shared with the web).
- `scripts/seed-demo-account.py` — provisions a local dev account with a tree,
  journal, and moments for simulator testing (needs the compose stack and the
  `pynacl` + `cryptography` Python packages).
- `project.yml` — the xcodegen definition.
